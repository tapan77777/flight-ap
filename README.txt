🚀 Flight Price API Backend (Travelpayouts Integration)

1. Install dependencies:
   npm install

2. Add your API token in the .env file:
   TP_TOKEN=your_real_token_here

3. Start the server:
   node server.js

4. Test API:
   http://localhost:5000/api/flight-price?origin=DEL&destination=BLR&date=2025-06-13
